using UnityEngine;

public class Stitch : MonoBehaviour
{

    //Stitch Health
    

    //Stitch health bar (UI Element)

    //



    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
